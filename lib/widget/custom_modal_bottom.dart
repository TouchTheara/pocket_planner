import 'package:flutter/material.dart';

class CustomModalBottom extends StatelessWidget {
  const CustomModalBottom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
